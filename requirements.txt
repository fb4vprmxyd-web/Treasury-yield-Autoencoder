# Core dependencies. Lower bounds are conservative; the code uses only standard
# APIs and runs on any reasonably recent scientific-Python stack.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
scipy>=1.10

# Tested with: numpy 2.4.4, pandas 3.0.2, matplotlib 3.10.8,
#              scikit-learn 1.8.0, scipy 1.17.1 (Python 3.12).
